# 存储后端（本地默认 sqlite：SQLite 索引 + JSON 内容）
# 可选：sqlite / file / memory / upstash / postgres
# RUSIN_STORAGE=sqlite

# 运行数据目录（默认 data/；Zeabur 等挂载持久卷时设为 /data）
# RUSIN_DATA_DIR=data

RUSIN_SECRET_KEY=

RUSIN_ADMIN=
